<article>
  <header>
    <figure>
      <img src="images/{{image}}" alt="{{imgAlt}}" />
    </figure>
    <h2 class="name">{{name}}</h2>
  </header>
  <p class="price">${{formatPrice price}}</p>
  </footer>
    <a href="#" class="add_cart">Add to cart</a>
  </footer>
</article>